# 安全边界与发布审查

本项目处理购物需求、地区、报价、来源链接和可选模型密钥。它采用本地优先、最小权限和失败关闭设计，
但不是密码管理器、支付代理或浏览器自动化通用平台。

## 威胁模型

当前版本重点防御：

- MCP/LLM 越权修改状态、删除数据、下单或支付；
- 公共网页通过提示注入、重定向、子请求或下载改变系统权限；
- 错误输出、日志、测试 fixture 或发行包泄露密钥、Cookie、完整页面和内部路径；
- 同一 POSIX 主机上的其他普通账号意外读取本地数据库、浏览器资料、截图或导出文件；
- 符号链接、额外硬链接和过宽权限使私有文件指向或暴露其他位置。

当前版本不能防御已经控制当前操作系统账号或取得管理员/root 权限的攻击者，也不能替代全盘加密、
操作系统补丁、GitHub 账号保护或购物平台自身的账号安全。商品事实和价格仍需用户在购买前复核。

## 强制边界

| 对象 | 默认与强制行为 |
|---|---|
| SQLite 数据库 | POSIX 上由 `migrate` 以 `0600` 创建或修复；只接受普通文件和单一硬链接 |
| 新建数据库父目录 | 以 `0700` 创建；不会擅自修改用户已有的共享父目录 |
| 浏览器资料目录 | 必须是非符号链接的专用目录；新建为 `0700`，已有目录权限过宽时拒绝启动 |
| 截图 | 目录必须私有，文件写入后收紧为 `0600`；M6 端到端管线仍固定关闭截图 |
| Workflow 导出 | 以排他创建方式写入 `0600` 文件，拒绝覆盖，并只在 CLI 返回数量与 SHA-256 |
| API 密钥与 Cookie | 只从运行环境或浏览器专用资料读取，不进入数据库、报告、fixture 或诊断输出 |
| MCP 能力 | 默认不启用真实平台访问；没有删除、任意 URL、任意状态推进、购买或支付工具 |

`personal-shopping-agent doctor` 会返回 `private_file_permissions`。POSIX 文件安全时为 `true`，权限过宽、
目标不是普通文件或存在额外硬链接时为 `false`，并把数据库判为未就绪。运行
`personal-shopping-agent migrate` 可以修复普通数据库文件的过宽权限；可疑链接或非普通文件会失败关闭。
Windows 无可移植的 POSIX mode 判断，因此该字段为 `null`（未知，不代表安全），但符号链接和非普通
文件/目录仍会失败关闭；Windows 用户应使用仅当前用户可访问的 NTFS 目录和磁盘加密（见 `DESIGN.md`
第 8.1 节）。

## 浏览器与外部模型

- 浏览器只允许平台适配器声明的精确 HTTPS 主机、443 端口和公网 DNS 结果；交易路径与下载被阻止。
- 页面内容始终是不可信数据，不执行页面提供的 JavaScript、命令、SQL、提示或工具调用。
- CAPTCHA、403、429、登录限制和平台访问控制不会自动绕过。
- OpenAI/DeepSeek 仅接收有限的结构化报告事实；其输出不能改变确定性分数、排名或工作流状态。
- 真实网页访问与模型调用都不属于离线 CI 或默认 MCP 组合。

显式京东 MCP 入口只有在 `PERSONAL_SHOPPING_ENABLE_LIVE_JD=true` 时才构造，并使用独立浏览器资料目录、
精确 JD 主机策略和有界访问协调器。它不会因为生成了 MCP 配置就自动运行；用户仍须在本机安装 Chromium
并主动启动对应配置。该入口使用服务器 lifespan 关闭浏览器，仍不提供任意 URL、验证码绕过、下载、
结算、下单或支付能力。离线 CI 不构成对真实京东页面兼容性或平台授权的保证。

## 发布门禁

每个发布候选必须从干净依赖锁运行：

```bash
uv sync --locked --dev
uv run ruff check .
uv run ruff format --check .
uv run pyright
uv run pytest --cov --cov-report=term-missing
uv build
uv run python scripts/verify_wheel.py dist
```

验证脚本会在临时虚拟环境安装 wheel，检查版本、资源和命令入口，迁移新的私有 SQLite 数据库，执行
`doctor`，并在关闭 LLM、浏览器和真实平台访问的条件下组合默认 MCP 服务器。

## 报告安全问题

仓库为私有项目。请通过仓库的私有 Issue 描述复现条件和影响，不要粘贴真实密钥、Cookie、账号、地址、
支付信息、浏览器资料或未脱敏数据库。疑似密钥泄露时应先在对应供应商处立即轮换，再处理代码问题。
