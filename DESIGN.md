# Personal Shopping Agent — Design Source of Truth

本文档记录项目的规范性设计。实现、测试和评审应以它为准；若实现与本文冲突，应先更新设计并说明迁移方案。

## 1. 目标与原则

本项目是本地优先的个人购物决策代理。它收集公开可访问的商品信息，在保留来源与时间语境的前提下，标准化参数和价格，再生成可解释、可复核的比较报告。

必须遵守以下原则：

1. 证据优先：事实必须绑定来源、采集时间和适用范围；未知就保持未知。
2. 决策可解释：每个筛选、分数、风险惩罚和结论都可追溯。
3. 模型受约束：LLM 只负责需求解析和自然语言解释；Pydantic 模型验证输出，确定性代码执行核心决策。
4. 安全优先：网页内容是不可信输入，不能把页面文字当作系统指令、代码或工具调用。
5. 人类最终决定：系统不执行购买、支付或不可逆的消费操作。
6. 小步交付：每个 PR 聚焦一个可验证问题，并附测试和变更说明。

## 2. 技术基线

- Python 3.12
- uv：依赖与虚拟环境管理
- `src/` 包布局
- Ruff：格式和静态规则
- Pyright：严格类型检查
- pytest：单元与集成测试
- Pydantic：边界模型和 LLM 输出验证（M1 起）
- SQLAlchemy 2.x、SQLite、Alembic：本地持久化（M1 起）
- 官方 MCP Python SDK v2 / `MCPServer`：工具接口（M2 起）
- Playwright Chromium：受控网页访问（M3 起）
- Jinja2：后续 HTML 模板报告（M5 后续切片）
- 系统 Keyring：敏感配置（需要账号能力时启用）

第一阶段采用模块化单体，不引入分布式服务。各层通过明确的 Python 接口隔离，以便未来替换平台适配器、存储或模型提供方。

## 3. 模块划分与分层

代码按“购物决策能力”划分为五个业务层，另有共享内核、共享技术设施和外层接口。每个业务层是
`src/personal_shopping_agent/` 下的一个包；技术适配器作为子包放在它服务的业务层内部。

| 包 | 模块 | 职责 |
|---|---|---|
| `intake/` | ① 明确用户需求层 | 把宿主整理出的结构化需求做确定性审查：列出会导致评分失败、必然无合格候选或降低可信度的缺口，并给出追问建议；提供宿主引导文本 |
| `sourcing/` | ② 自动查找对应商品层 | 搜索候选、受限详情采集、平台观察、Product/Offer 转换、官方规格核验与规格规范化；适配子包 `browser/`（受控 Playwright）与 `platforms/`（京东解析器） |
| `presentation/` | ③ 呈现待购商品层 | 条件效用、报价成本、证据置信度、最终排名、确定性报告、报告读取与可选解释端口；适配子包 `rendering/`（安全 HTML）与 `llm/`（OpenAI / DeepSeek） |
| `automation/` | ④ 自动化层 | 工作流生命周期、可恢复端到端管线，以及以单个 Workflow 为边界的数据导出/删除 |
| `evolution/` | ⑤ 自动进化层 | 在本地生成只含稳定代码和结构信息的脱敏改进案例预览，供用户确认后交给维护型 AI；不上传、不改代码 |
| `domain/` | 共享内核 | 实体、值对象、确定性工作流状态机、规格计量目录和条件边界规则 |
| `infrastructure/` | 共享技术设施 | SQLite 存储与迁移、私有本地文件边界、进程级设置 |
| `interfaces/` | 外层接口 | MCP 服务器、CLI、健康检查、宿主配置生成与组合根 |

依赖方向只能由外向内：

1. `domain` 不依赖任何其他项目包，也不得导入 Playwright、MCP SDK、SQLAlchemy、Jinja2 或模型 SDK；
2. `intake` 与 `evolution` 只依赖 `domain`（`evolution` 另可读取版本元数据）；
3. `sourcing` 依赖 `domain`；`presentation` 依赖 `domain` 和 `sourcing` 产出的规范事实与核验契约；
4. `automation` 依赖 `domain`、`intake`、`sourcing`、`presentation` 的核心用例；
5. 业务层核心模块不得导入 `infrastructure`、`interfaces` 或任何适配子包，也不得导入上述第三方
   技术 SDK；只有 `sourcing.browser`、`sourcing.platforms`、`presentation.llm`、`presentation.rendering`
   可以使用各自需要的技术依赖；
6. `infrastructure.storage` 实现各业务层声明的端口，只依赖 `domain` 与业务层核心契约；
7. `interfaces` 是唯一组合根，也是唯一可以同时装配存储、浏览器、模型与业务用例的位置；
8. 除 `interfaces` 外，任何包不得依赖 `evolution`，运行时业务流程因此无法触发改进流程。

平台解析器不得直接决定排名。上述规则由 `tests/unit/test_architecture.py` 以 AST 静态检查强制执行，
新增包或模块时必须同时更新该测试中的层级表。下文沿用的“Application”一词指各业务层中不含技术适配
子包的核心用例模块。

### 3.1 运行时 AI 与维护型 AI 隔离

项目区分两个权限完全不同的 AI 通道：

1. **运行时 AI**位于 MCP 宿主中，把自然语言整理为类型化输入并调用已注册工具。它只能获得工具明确
   声明的业务能力，不得写仓库、修改评分/证据/安全规则、移动 Git 引用、合并 PR、发布版本或扩大自身
   权限。网页、模型输出和数据库内容都不能触发代码修改。
2. **维护型 AI**位于 GitHub/Codex 等开发环境，只根据项目所有者确认的需求和脱敏复现案例在独立分支
   修改代码。每次修改必须补充测试，经过静态检查、构建、隔离安装、PR 审查和人工合并；不得自动读取
   真实购物数据库、Cookie、密钥、地址或浏览器资料，也不得自动合并或发布。

运行时问题若要进入改进流程，必须先形成由用户预览和确认的最小脱敏案例。默认不自动上传运行数据。
持续改进以解析成功率、证据完整度、可恢复性、报告可复现性、安装成功率和用户明确反馈等量化指标
验收；“模型输出更自然”不能替代事实、测试和安全门禁。具体流程见 `docs/AI_EVOLUTION.md`。

### 3.2 明确用户需求层

MCP 宿主负责把自然语言整理成结构化输入；`intake` 负责在任何存储或网络访问之前，用与评分阶段
完全相同的确定性规则审查该输入。审查只读取请求本身，不调用模型、不猜测缺失值，也不改写用户条件，
结果是 `RequirementReview`：

- `blocking` 问题：条件边界非数值、为负、上下限颠倒、偏好值越界、没有 `minimum`/`maximum` 方向，
  条件键在规范化后重复，或硬性条件使用了不受支持的规格键/单位。前几类会让评分阶段直接失败，最后
  一类会让所有候选必然被排除；
- `warning` 问题：没有任何条件、软性条件的规格键不受支持、单位缺失或与规范单位不一致（该条件只能
  得 0 分），以及地区未指定；
- 每个问题携带稳定代码、条件键和可直接转述给用户的追问建议。当条件键或单位恰好是规格目录中的
  声明别名时，建议给出规范键或规范单位，但仍由用户确认后重新提交。

规格计量目录（规范键、别名、规范单位与换算系数）和数值条件边界规则属于 `domain`，由需求审查、
规格规范化和条件评分共用，保证三处口径一致。`start_shopping_workflow` 在存在 `blocking` 问题时拒绝
创建工作流；只读、幂等、不访问网络的 `review_shopping_request` 工具让宿主先审查再追问。端到端管线从
`request_validated` 启动时同样先执行审查，发现阻断问题即在任何平台访问之前停止，工作流保持原状态。

### 3.3 自动进化层

自动进化层只实现 `docs/AI_EVOLUTION.md` 中“本地反馈与回放”的第一步：从一个已持久化 Workflow 在本地
生成 `ImprovementCase` 预览。案例由构造保证脱敏，只能包含：

- 软件版本、工作流当前状态、失败或停滞阶段、稳定错误码；
- 由事件理由组成的阶段轨迹（均为项目定义的代码）；
- 请求结构摘要：条件数量、受支持规范键列表、不受支持条件数量、是否设置地区与弹性预算、预算币种；
- 由状态推导的固定期望/实际表现说明、空的 fixture 引用和默认验收检查。

案例不得包含查询文本、品类、地区、自定义条件键、金额、商品名、卖家、URL、报告正文或任何页面内容；
所有字符串字段都限制为代码格式。用户可额外提供一个同样受代码格式约束的错误码。该能力只作为本地
CLI `improvement-case` 输出到标准输出，带明确的隐私提示，不写文件、不访问网络、不注册 MCP 工具。
是否把预览粘贴到私有 Issue 由用户决定；后续代码修改仍只能由维护型 AI 在独立分支完成并经人工合并。

## 4. 核心领域不变量

### 4.1 Product 与 Offer 必须分离

`Product` 表示稳定的商品身份与规格；`Offer` 表示某个销售渠道在某个时间、地区、卖家和 SKU/变体下的报价。价格不得直接挂在一个没有渠道语境的 Product 上。

每个 Offer 至少保留：

- 平台、卖家、店铺类型和商品 URL；
- SKU/变体、地区、库存语境和采集时间；
- 展示价、无条件价、条件价与预计总成本中可获得的字段；
- 优惠条件、运费、安装/服务成本、会员要求和可验证证据。

### 4.2 价格分层

价格字段不能混用：

1. `list_price`：标价或参考价；
2. `displayed_price`：页面直接展示的当前价；
3. `unconditional_price`：无需领券、会员或组合条件即可获得的价格；
4. `conditional_price`：满足明确条件后的价格及条件说明；
5. `estimated_total_cost`：商品、运费、必要服务和可量化条件成本的估算。

默认比较 `estimated_total_cost`。无法计算时允许降级，但报告必须明确采用了哪一层价格以及缺失项。

### 4.3 证据与冲突

证据优先级默认如下：

1. 品牌/制造商官方材料；
2. 平台自营或品牌官方旗舰店；
3. 授权经销商；
4. 普通第三方商家；
5. 独立测评；
6. 用户评论；
7. 搜索摘要或无法打开的二手片段。

优先级不等于绝对真实。系统还要考虑新鲜度、字段适用范围、来源间一致性和证据完整度。冲突事实并存记录，不得静默覆盖；缺失事实保持 `unknown`。

### 4.4 本地持久化契约

M1 使用 SQLite 保存结构化请求、商品、报价与证据。领域对象先通过 Pydantic 严格校验，再以可迁移的 JSON 快照和必要索引字段写入数据库。读取时必须重新执行领域校验，数据库记录不能绕过领域不变量。

- SQLite 只保存本地决策所需信息，不保存密码、Cookie、支付数据或可复用会话；
- 数据库结构只能通过 Alembic 迁移演进；
- `offers.product_id` 必须引用存在的 Product；
- Evidence 采用多态主体引用，允许同一主体字段保存多个观察以表达冲突；
- JSON 快照用于保持领域契约完整，索引列只承担查询职责，不成为第二套事实来源。

最终候选分数使用 `candidate_scores` 追加保存；每条记录必须同时引用请求、工作流和 Product，且同一
工作流内每个 Product 只能有一条分数。索引列只保存资格、名次、Pareto 前沿、综合分和评分时间，
能力评估、报价成本、证据置信度、排除原因和所有公式输入继续保存在经过重新校验的完整 JSON 快照中。

确定性报告使用 `shopping_reports` 保存；同一工作流只允许一份当前方法版本的报告。索引列保存格式、
生成时间和内容 SHA-256，完整 JSON 快照同时包含原始请求、候选分数、Product、实际最佳 Offer、参与
评分的 Evidence 与已转义 Markdown。报告与 `report_rendered` 事件必须原子提交，不能只有状态而没有
内容，或只有内容而没有审计事件。

平台搜索和详情解析结果先作为 `SearchObservation` 与 `DetailObservation` 追加保存，并通过
`request_id` 绑定触发采集的购物请求。该观察表只保存经过严格模型校验的结构化字段，不保存
原始 HTML、截图路径、Cookie 或浏览器会话。相同 SKU 在不同时间得到的不同价格、库存或规格
观察必须分别保留，读取时重新校验完整载荷。

平台观察不等于领域事实：搜索观察永远不能直接形成 Offer；详情观察也只有在品牌/型号、
SKU/变体、卖家、地区、采集时间和至少一个可用价格层满足后续领域转换规则时，才可由独立用例
生成 Product、Offer 与 Evidence。仅仅写入观察表不得推进购物工作流。

搜索与详情采集使用一个请求级 SQLite 工作单元。它从已验证工作流读取 `request_id`，在同一
Session/连接内追加一次搜索观察、有限数量的详情观察和一个审计事件，并只在所有采集与校验成功
后提交。任何搜索、详情、持久化或并发修订失败都会回滚整个批次，并在 `finally` 语义下关闭连接。
该批次只把工作流推进到 `candidates_discovered`；`offers_collected` 必须留给后续真正生成并保存
Product/Offer 的事务，不能把详情观察冒充报价。

详情转换只读取与当前请求严格匹配的持久化观察，并要求品牌、型号、卖家、地区、库存语境、SKU
和至少一个页面可见价格层完整。条件价没有明确条件、选中变体与 SKU 不一致、下架与库存状态冲突
或标题无法进入领域约束时，整个转换批次失败。转换规则不得推测无条件价或预计总成本。

同一批次内按品牌与型号归并稳定 Product；每一次卖家、SKU、变体、地区和采集时间组合分别形成
Offer。页面规格、价格、店铺依据、库存与促销作为独立 Evidence 追加保存，并通过
`origin_observation_id` 回指原始结构化详情观察。平台页面证据在交叉核验前的 reliability/freshness
保持未评估，不能预填看似精确的置信分数。未知店铺只归为中性的 `platform_listing` 来源，不能升级
为第三方授权或官方来源。

Product、Offer、Evidence 与 `offers_collected` 审计事件必须在一个 SQLite 事务内提交。Product 父项
可在事务内先刷新以满足 Offer 外键，但任何后续错误仍回滚所有实体与状态；只有完整提交后工作流
才进入 `offers_collected`。

官方规格核验通过平台无关的异步提供端口获取制造商控制页面的结构化观察；当前只定义端口和离线
测试替身，不默认启用真实厂商网站访问。官方观察必须与目标 Product 的品牌和型号在大小写、空白
归一化后精确一致，否则整个核验批次失败，不以模糊匹配猜测产品身份。

核验以字段为单位追加保存 `match`、`conflict`、`platform_only`、`official_only` 或
`missing_official_source`，并同时保留两侧原始值和官方来源 URL。比较阶段只折叠大小写与连续空白，
不换算单位、不解析容量或重量，也不把部分交集或同侧多个不同值视为完全一致；这些标准化规则属于 M4。官方来源
缺失不是核验通过，即使平台规格也缺失，仍保存 `official_source` 缺失记录以便复核。

平台 Evidence 通过 `request_id` 绑定购物请求。官方 Evidence、字段级核验记录和
`evidence_cross_checked` 审计事件必须在同一 SQLite 事务内提交；提供端错误、官方身份不匹配、数据
校验、外键或并发修订失败时全部回滚。官方来源的 reliability/freshness 在 M4 评分前仍保持未评估，
不能仅因来源类型而填入虚假的精确置信分。

## 5. 决策与评分

所有中间量必须限制在定义域内，并在报告中解释权重、缺失处理和惩罚原因。

### 5.1 规格规范化

M4 首个切片把请求关联的 Product 规格 Evidence 转换为追加式 `NormalizedSpecification`，不修改或
覆盖原 Evidence。每条结果保留 Product、请求、工作流、原字段路径、所有不同原值、证据 ID、规范
字段、规范单位、所有不同规范值、处理状态和时间。初始规范字段限于：

- `battery_capacity`，统一为 `mAh`；
- `weight`，统一为 `g`；
- `display_size`，统一为 `inch`；
- `storage_capacity` 与 `memory_capacity`，统一为 `GB`，容量换算采用二进制口径
  `1 TB = 1024 GB`、`1 MB = 1/1024 GB`。
- `chip_performance`，单位 `SOCPK`：极客湾 SOCPK 综合性能分（CPU 70%、GPU 30%，骁龙 865 = 100），
  只来自 7.3 的独立评测证据，不从平台文字推测。

字段只接受声明过的中英文精确别名；数值只接受完整的非负“数字 + 单位”表达。系统不得从上下文
补单位、解释“约”“大概”等模糊词或猜测未知字段含义。单位换算后的唯一值标记为 `normalized`；
多个不同值标记为 `conflict`；已知字段但任一原值无法完整解析时标记为 `unparseable_value`；未知字段
标记为 `unsupported_key`。冲突和解析失败仍可保存已经确定的换算结果，但不能伪装成单一确定事实。

规范化只允许从 `evidence_cross_checked` 进入。所有规范结果与 `data_normalized` 工作流事件必须在
同一 SQLite 事务内提交；没有任何规格 Evidence、领域校验、外键或并发修订失败时全部回滚。完成
表示规范化过程已执行并显式记录缺失/冲突，不表示每个字段都有确定值。

### 5.2 条件效用与报价比较基础

M4 第二个切片只生成 `CandidateScoringFoundation`，作为证据置信度和最终排名的输入，不保存最终
分数，也不推进 `candidates_scored`。计算按请求和 Product 严格过滤规范事实；其他请求或商品的
事实不得参与当前候选评分。

数值条件必须至少声明 `minimum` 或 `maximum`，所有边界均为非负 Decimal，并满足
`minimum <= preferred <= maximum`。只有 `preferred` 而没有方向边界时拒绝评分。条件单位必须与
规范事实的单位完全一致；缺少单位、单位不一致、事实缺失、冲突或无法解析都不由系统猜测，分别
保留明确状态并记为 0 分。确定值 `v` 的能力分按以下规则计算并四舍五入到六位小数：

- 低于最小值：`0.5 × v / minimum`，状态为未满足；
- 高于最大值：`0.5 × maximum / v`，状态为未满足；
- 处于边界内且没有偏好值：1；
- 从最小值到偏好值：由 0.5 线性增加到 1；
- 从偏好值到最大值：由 1 线性降低到 0.5；
- 未设置边界的有利一侧保持 1。

各项仍使用下式聚合；硬性条件只有状态为 `satisfied` 才算通过，不能靠其他条件的高分抵消。
对于用户准则 `i`，权重为 `w_i`、归一化能力分为 `s_i ∈ [0,1]`：

```text
Utility = Σ(w_i × s_i) / Σ(w_i)
```

报价首先按 4.2 的顺序选择当前可获得的最高质量价格层，再计算
`EffectiveCost = selected_price × (1 + risk_coefficient)`。这里的系数只表达信息不确定性，不是
对卖家真伪或商品质量的判断；所有增量相加后最高限制为 0.30：

| 维度 | 情况 | 增量 |
|---|---|---:|
| 店铺语境 | 平台自营 / 品牌旗舰 / 授权零售 / 第三方 / 未知 | 0 / 0.01 / 0.02 / 0.06 / 0.08 |
| 价格层 | 预计总成本 / 无条件价 / 展示价 / 条件价 / 标价 | 0 / 0.01 / 0.04 / 0.08 / 0.10 |
| 库存 | 未知 / 明确缺货 | 0.04 / 0.10 |
| 地区 | 缺失 | 0.03 |
| 条件价 | 缺少条件说明 | 0.05 |

明确缺货、条件价缺少条件说明或币种与请求预算不一致时，该报价不可比较；币种不做自动换算。
其余报价按调整后成本升序选择最佳可比报价，相同成本用 Offer ID 作稳定决胜。缺失的运费、服务费
或优惠不得被推测为 0。该最佳报价只是后续评分输入，不等于最终购买建议。

### 5.3 证据置信度

M4 第三个切片生成纯 `CandidateEvidenceConfidence`，不修改原 Evidence、不持久化结果，也不推进
`candidates_scored`。`CandidateScoringFoundation` 必须携带并严格匹配当前 `request_id`；规范事实、
Evidence 和字段核验记录还必须同时匹配请求与 Product。其他作用域的数据不得参与计算，参与计算的
未来时间记录和重复 Evidence ID 直接拒绝。

每个条件只有在能力评估状态为 `satisfied` 或 `unsatisfied` 时才算事实完整；`missing`、`conflict` 和
`unresolved` 均保持不完整。`Completeness` 是全部条件中按权重计算的完整比例。为避免同一缺失被
重复惩罚，来源可靠性、新鲜度和一致性只在事实完整的条件之间按权重平均；没有完整条件时三项均为 0。

来源可靠性取该条件所链接证据中的最高可用值。Evidence 已有显式 `reliability` 时使用显式值，否则
采用以下可替换的来源类型先验。这些数值只表示默认证据质量，不证明真伪、授权关系或商品质量：

| 来源 | 制造商官方 | 平台自营 | 品牌旗舰 | 授权零售 | 独立测评 | 第三方卖家 | 平台列表 | 用户评论 | 搜索摘要 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 默认可靠性 | 0.95 | 0.90 | 0.85 | 0.80 | 0.75 | 0.65 | 0.60 | 0.45 | 0.30 |

新鲜度同样取链接证据中的最高可用值；显式 `freshness` 优先。当前只评估相对稳定的 Product 规格，
未显式评估时按采集年龄使用：180 天内 1、181–365 天 0.90、366–730 天 0.80、超过 730 天 0.70。
报价的新鲜度需在最终排名事务中采用更短时效口径，不能复用该规格规则。

一致性按以下优先级处理：

1. 事实未解决或没有任何可链接 Evidence：0；
2. 至少两个不同来源 URL 经规格规范化得到同一确定值：1；同一 URL 的重复分类不算独立来源；
3. 单来源但字段核验为 `match`：1，`conflict`：0；
4. `platform_only`：0.60，`official_only`：0.75，`missing_official_source`：0.50；多个单侧状态取最低值；
5. 没有相关字段核验的单来源：0.50。

第二条允许 M4 的严格单位换算纠正 M3 原始文本比较产生的表面冲突，例如 `6 Ah` 与 `6000 mAh`；
前提是规范记录确实引用两个不同来源并得到唯一值。所有分量、中间聚合及调整后效用四舍五入到六位。

证据置信度：

```text
EvidenceConfidence = Completeness × SourceReliability × Freshness × Consistency
AdjustedUtility = Utility × EvidenceConfidence
```

该输出仍只是排名输入，不包含报价价值指数、综合分、Pareto 前沿或最终推荐。

### 5.4 最终排名

最终排名只接受作用域一致、时间有效的 `CandidateScoringFoundation` 和
`CandidateEvidenceConfidence`。请求条件的规范化键、权重、硬性标记和顺序必须与评分基础完全一致；
最佳报价币种必须等于预算币种。评分时间必须带时区，证据置信度不得来自评分时间之后。

候选先执行资格闸门。出现以下任一情况时保留分数快照和排除原因，但不生成价值指标、综合分、
Pareto 前沿或名次：

- 请求没有可评分条件；
- 任一硬性条件未满足；
- 没有可比最佳报价，或有效成本不大于 0；
- 证据置信度为 0；
- 选定价格超过弹性预算；没有弹性预算时，超过正常预算即排除。

预算层以最佳报价的 `selected_price` 判断，而不是以风险调整后的成本判断。价格不高于正常上限时为
`within_budget`；只在正常上限与可选弹性上限之间时为 `within_stretch`；更高时为 `over_budget`。
正常预算层始终排在弹性预算层之前，避免轻微的能力优势默认替用户突破预算。风险仍通过
`EffectiveCost` 与 `RiskPenalty` 单独表达：

```text
EffectiveCost = selected_price × (1 + risk_coefficient)
ValueIndex = AdjustedUtility / EffectiveCost
ValuePer100 = 100 × ValueIndex
RiskPenalty = 100 × risk_coefficient
```

面向最终展示的综合分：

```text
FinalScore = clamp(0, 100,
  100 × (0.75 × Utility + 0.25 × EvidenceConfidence - risk_coefficient)
)
```

`ValueIndex` 四舍五入到 12 位小数；`ValuePer100`、`RiskPenalty` 和 `FinalScore` 四舍五入到 6 位。
综合分用于稳定排序和展示，不取代能力、证据、成本和风险的分项解释。

正常预算层与弹性预算层分别计算 Pareto 前沿。支配关系同时要求：调整后效用不低、证据置信度不低、
有效成本不高，并且至少一项严格更好。未被支配的候选为第 1 前沿，移除后重复计算后续前沿。最终名次
按以下顺序稳定排序：预算层、Pareto 前沿、综合分降序、每百元价值降序、有效成本升序、Product ID。
完整并列时 Product ID 只承担可复现的决胜，不表达业务偏好。

最终候选快照与 `candidates_scored` 事件必须在一个 SQLite Session/事务中提交。事务只读取当前请求、
当前工作流的规范事实和核验记录，以及通过请求 Evidence 明确绑定的 Product 与 Offer；不能把其他请求
或旧工作流的数据混入。没有候选、状态错误、模型校验、外键或并发修订失败时全部回滚，工作流保持在
`data_normalized`。边际升级价值属于 M5 报告解释层，可由这些已保存指标计算，但不得反向改写 M4 名次。

### 5.5 报告与 LLM 边界

报告基础层不绑定 ChatGPT、OpenAI API 或任何其他模型厂商。ChatGPT 可以作为 MCP 宿主与用户对话，
这与项目内部嵌入一个 LLM 提供方是两个独立层次。M5 第一个切片不得调用 LLM：它只从持久化的请求、
候选分数、Product、最佳 Offer 和实际参与评分的 Evidence 构建 `ShoppingDecisionReport`，再由确定性
渲染器输出 Markdown。即使没有网络或模型 API，用户仍应得到完整、可复核的比较报告。

报告必须展示需求与预算、合格候选排名、预算层、选定价格与有效成本、综合分、每百元价值、证据
置信度、Pareto 前沿、逐项能力/证据分、实际来源 URL、排除原因、采集时间和明确免责声明。没有合格
候选时必须明确说明，而不是强行推荐。Product、Offer、Evidence 与 CandidateScore 的请求、工作流和
主体标识必须精确一致；引用缺失、重复或多余输入都拒绝生成报告。

平台标题、卖家名、商品名、用户需求及其他外部字符串均是不可信文本，进入 Markdown 前必须转义，
不得注入标题、链接、HTML 或隐藏指令。报告 UTF-8 内容计算 SHA-256，并与完整 Pydantic 快照一起保存；
读取时重新校验内容哈希。报告与 `report_rendered` 工作流事件使用同一 SQLite Session/事务，任一校验、
外键或并发错误时整体回滚到 `candidates_scored`。

后续 LLM 解释器必须通过模型无关端口接入；OpenAI/ChatGPT、DeepSeek 或本地模型只是可替换适配器。
模型只能读取已验证报告快照，并返回受 Pydantic 约束的解释字段；不得新增事实、修改公式、重排候选、
隐藏不确定性或生成购买/支付动作。模型不可用、输出校验失败或未配置 API 时，确定性报告是强制回退。
ChatGPT 订阅不等同于 OpenAI API 授权，任何提供方密钥都不得进入仓库或报告快照。

M5 第二个切片将该边界实现为 `ReportExplanationProvider`。应用不把已转义 Markdown、原始 HTML、截图、
Cookie、页面来源文字或 Evidence URL 整体发送给模型，而是从报告快照投影有限的 `ExplanationFact`。
默认选择报告顺序中的前三个候选，调用方最多可在 1–10 之间调整；每项事实使用稳定 ID，并绑定可选的
Product ID。投影范围包括需求、预算、确定性推荐、固定免责声明，以及候选的资格、名次、预算层、
选定价格、有效成本、综合分、每百元价值、证据置信度、风险惩罚、Pareto 前沿、排除原因和逐项条件分。
外部商品名和用户需求仍是不可信数据，提示中必须明确把所有 fact value 当作数据而非指令。

解释输出只允许总览、按原顺序逐候选说明和限制提醒；每段必须引用请求中的事实 ID，且候选段不能引用
其他 Product 的事实。输出必须回显报告 ID 与内容 SHA-256，候选集合及顺序必须与请求精确一致，限制
提醒必须引用固定免责声明。模型生成的链接一律拒绝，来源链接继续只由确定性报告展示。事实 ID 引用
只能提供可复核边界，不能在数学上证明自然语言与事实语义完全一致，因此确定性报告始终是权威结果，
解释只能作为非持久化覆盖层，不修改报告、不推进工作流。

OpenAI 适配器使用 Responses API Structured Outputs 直接解析 Pydantic 模型；DeepSeek 适配器使用其
OpenAI 兼容 Chat Completions JSON Output，再执行同一 Pydantic 与应用范围校验。DeepSeek 不需要视觉
能力，因为该端口只消费结构化文本事实；未来若需要商品图片理解，应由独立受控视觉/采集适配器先生成
带来源的结构化事实。任一提供方超时、认证/网络失败、空响应、JSON/Schema 错误或本地范围校验失败，
统一返回脱敏原因码并保留确定性报告。模型名必须显式配置，API 密钥只从运行时内存传入 SDK；适配器
不得记录密钥、原始提供方异常或响应正文。SDK 自动重试关闭，首个切片不做隐藏重试或真实 API 测试。

为复用供应商维护的认证、超时和协议实现，本切片新增官方 `openai` Python SDK。OpenAI 直接使用该
SDK；DeepSeek 依据其公开的 OpenAI 兼容接口复用同一客户端并固定官方 Base URL。该 SDK 仅存在于外层
LLM 适配器，Application 与 Domain 不得导入它。

M5 第三个切片增加运行时选择与用户可见展示，但不把提供方配置放入 Application。安全默认值为
`PERSONAL_SHOPPING_LLM_PROVIDER=disabled`；只有明确选择 `openai` 或 `deepseek` 后，外层配置模块才
分别读取 `OPENAI_API_KEY` / `PERSONAL_SHOPPING_OPENAI_MODEL` 或 `DEEPSEEK_API_KEY` /
`PERSONAL_SHOPPING_DEEPSEEK_MODEL`。未选中提供方的环境变量不得读取进设置对象。模型名不设代码默认值；
超时、最大输出 token 和候选数分别限制在 `(0,120]`、`[256,4096]` 和 `[1,10]`。未知提供方、缺少
密钥/模型或数值越界必须在创建 SDK 客户端前失败，并只暴露稳定的脱敏错误码。

运行时设置中的密钥使用 `SecretStr`，从 `repr` 和 `model_dump` 中排除，只在构造已选适配器时短暂取值。
组合工厂创建 `ShoppingReportExplanationService` 与 `ShoppingReportPresentationService`，Application
继续只依赖模型无关端口。该工厂不代表 MCP 已启用真实模型；在报告读取/展示用例注册为 MCP 工具并
完成人工配置前，不得把内部适配器能力报告为可直接使用的 MCP 能力。

成功解释的展示必须先包含原确定性 Markdown 的完整字节，随后以分隔线追加明确标注为“AI 辅助解释”
的章节。模型文本、提供方、模型名、Product 名、事实标签和值再次经过统一 Markdown 转义；候选标题
来自确定性报告而不是模型。每段后展示已经过范围校验的事实 ID 及其精确值，整个组合内容重新计算
SHA-256。`RenderedShoppingReportPresentation` 必须校验基础报告前缀与新哈希。关闭、未配置、提供方
不可用或输出无效时，不追加任何标题、错误文本或模型信息，展示内容和哈希必须与原确定性报告完全
相同。解释展示仍不持久化、不推进工作流，也不能反向成为 Evidence 或 CandidateScore。

M5 第四个切片把已经存在的报告能力接入 MCP，同时保持模型调用显式可见。SQLite 报告读取适配器按
Workflow ID 查询唯一报告，先重新校验完整 `RenderedShoppingReport` 载荷，再核对报告、请求、工作流、
格式、内容哈希与正文索引；索引和载荷不一致时拒绝返回，不能选择看起来较新的那一份作为事实。
Application 通过 `ShoppingReportReader` 和 `ShoppingReportAccessService` 隔离 MCP 与 SQLAlchemy。

MCP 分别暴露三个边界清晰的工具：`render_shopping_report` 只允许已有 `candidates_scored` 结果的工作流
生成并原子保存确定性报告；`get_shopping_report` 只读取本地持久化报告，必须声明为只读、幂等且不访问
开放网络；`explain_shopping_report` 是唯一会显式请求可选模型解释的工具，声明为只读、非幂等且可能
访问开放网络。普通报告读取永远不能调用 LLM。解释工具在提供方关闭、不可用或输出无效时仍返回与
存储报告逐字节一致的回退，不保存模型文本，也不推进状态。

生产 MCP 组合在启动时读取一次经过校验的 LLM 设置并报告当前提供方；默认仍为 `disabled`。能力状态
必须如实区分“可为已经评分的工作流生成/读取报告”和“尚未暴露平台采集、评分 MCP 工具”，不得因为
内部已实现 M3/M4 服务就宣称 MCP 已能自行完成整条购物流程。MCP 仍不得提供任意状态推进、下单或支付
工具。

M5 第五个切片完成确定性 HTML 报告。持久化的 Markdown 及其完整报告快照继续作为唯一规范报告；HTML
不新建第二份数据库事实，而是由 `ShoppingReportAccessService` 从同一已验证
`ShoppingDecisionReport` 按需派生。这避免修改工作流状态、重复报告记录或 Markdown/HTML 快照漂移。
HTML 使用独立 `ReportFormat.HTML` 与自己的 UTF-8 SHA-256，生成时间仍等于原报告时间。

HTML 适配器使用 Jinja2 3.1 系列的包内模板、`SandboxedEnvironment`、`StrictUndefined` 和默认开启的
自动转义。模板是开发者控制的静态资源，用户需求、商品名、卖家、来源标题等外部文字只能作为数据
进入模板。输出是无 JavaScript、无表单、无图片、无字体和无外部 CSS 的单文件文档，并带 CSP、
`no-referrer` 及安全外链属性。模板必须展示与 Markdown 相同的需求、预算、结论、排名、价格、风险、
条件分、证据、排除原因和限制；缺失与无合格候选仍需明确显示。

`get_shopping_report_html` 是只读、幂等且不访问开放网络的 MCP 工具；它不调用 LLM，也不把 HTML 写回
数据库。可选模型解释继续通过独立的 Markdown 解释工具提供，不混入确定性 HTML，以保持 HTML 导出
完全可复现。模板必须随 Python wheel 打包并在发布验证中检查。

#### 5.5.1 MCP 紧凑输出（M9）

MCP 工具的结构化输出会作为工具定义（输出 Schema）和每次调用结果进入宿主模型上下文。完整报告快照
（每个候选的 Product、Offer、Evidence 与全部评分输入）的输出 Schema 每个约 25 KB，单候选结果约 25 KB，
而同一事实的已转义 Markdown 报告只有约 2 KB。因此 M9 起：

- 报告相关工具与端到端管线只返回紧凑视图：工作流 ID、报告 ID、格式、候选数、合格候选数、确定性推荐
  Product ID、内容 SHA-256、生成时间和报告正文（Markdown 或 HTML）；解释工具另返回状态、回退原因、
  提供方与模型名；
- 视图只能从服务端已完整校验的 `RenderedShoppingReport` / `RenderedShoppingReportPresentation` 构造，
  正文与哈希逐字节保持不变；完整 JSON 快照继续保存在本地数据库，并可用 CLI `data export` 导出复核；
- `run_shopping_pipeline` 直接返回报告正文，宿主无需再调用一次 `get_shopping_report`。

### 5.6 M6 可恢复端到端装配

M6 第一个切片新增应用层 `ShoppingDecisionPipelineService`，但不绕过任何既有用例或事务。它先读取
持久化工作流状态，再从该状态对应的唯一后继阶段继续：`request_validated` 执行京东搜索与受限详情
采集，随后依次执行 Offer 转换、官方证据核验、规格规范化、候选评分、确定性报告和完成事件。只有
真实用例成功并提交后才记录对应状态；某阶段抛错时保留上一个已提交状态，调用方可以重试，而不能
通过管线参数跳级、倒退或指定目标状态。`failed` 和 `request_received` 不可进入该管线。

采集参数只允许 1–100 个搜索候选和 1–10 个详情，且详情上限不得大于候选上限；管线固定
`screenshot=false`。从 `report_rendered` 恢复时必须先完整校验已存报告，再提交 `completed`；从
`completed` 重入只返回同一报告和空的已执行阶段列表，不再次访问外部来源。各阶段继续使用原有的
SQLite 工作单元，因此一次阶段内的数据与审计事件共同提交，跨阶段不伪装成一个长事务。

生产默认组合仍只创建 M5 本地工作流与报告服务器。M6 工具只能通过显式京东组合工厂注册，并要求
调用方注入实现安全导航、限频和生命周期管理的 `PageCollector`，以及明确的
`OfficialEvidenceProvider`。显式的空官方提供器只能产生“官方来源缺失”记录，不能补写推测值。该组合
新增的 `run_shopping_pipeline` 标记为写入、非破坏、非幂等和开放网络；它不接受任意 URL，不暴露浏览
器对象，也没有下单或支付能力。普通服务器必须继续如实报告未配置端到端及平台采集能力。

MCP SDK 会按返回类型生成 Decimal 的普通十进制字符串 Schema。所有工作流、报告、解释和管线顶层
返回契约因此在 JSON 模式递归使用无指数 Decimal 字符串，既保持十进制精度，也避免内部规范化值如
`6E+3` 与公开 Schema 不一致。离线 SDK 集成测试必须覆盖完整阶段后缀、完成后重入、失败状态拒绝、
工具注解、能力状态和实际搜索/详情 fixture 调用次数。

M6 第二个切片建立可安装的数据库管理边界。Alembic 历史必须作为包资源进入 wheel，运行时通过包内
路径创建配置，不能依赖当前工作目录存在源码仓库或 `alembic.ini`。`doctor` 只能读取数据库存在性、
当前 revision 和包内目标 revision；检查一个缺失的文件型 SQLite 数据库不得创建文件或父目录。
`migrate` 只允许本地 SQLite，并只运行到包内 `head`，不得提供自动 downgrade、清库或重建命令。

CLI 诊断输出只能包含服务版本、命令、是否存在、revision、ready 状态和稳定脱敏错误码，不回显数据库
URL、路径或底层异常。默认 MCP stdio 入口必须先要求数据库达到当前 head；缺失或落后时立即拒绝启动并
提示用户显式运行迁移，不能在模型工具调用过程中隐式修改 Schema。底层依赖注入工厂仍接受测试或高级
调用方管理的已建库。CI 必须构建 sdist 与 wheel，并从 wheel 解压内容完成一次全新数据库迁移和版本
检查，以防源码测试通过但发行包漏带迁移或模板。

M6 第三个切片把版本与安装验收收敛为发行契约。版本只在包内无依赖元数据模块声明，Hatch 构建元数据、
Python 包、健康检查和 MCP 元数据都读取同一值。CI 从 `dist` 自动定位唯一 wheel，检查版本、迁移、模板
和两个 console entry point 后，在全新临时虚拟环境中安装 wheel 及依赖；随后实际执行 health、迁移前
doctor、migrate、迁移后 doctor，并从安装位置组合默认 MCP。验收过程强制关闭 LLM，不启动浏览器或访问
购物平台。

M6 第四个切片建立结构化本地数据的立即导出与删除边界。导出单位是一个 Workflow 及其唯一 Request，
包含重新验证的事件、结构化平台观察、Evidence、核验、规范化、评分、报告，以及它们实际引用的
Product/Offer。JSON 包带格式版本、软件版本、导出时间和完整文件 SHA-256；目标文件使用仅当前用户可读写
权限创建，已存在路径一律拒绝覆盖。CLI 只返回数量、字节数、哈希和隐私提醒，不把查询、地区、卖家、
URL 或报告正文打印到诊断输出。导出文件可能包含这些信息，必须由用户自行私密保管。

删除命令默认只生成预览，列出各类删除数量、因其他任务可能引用而保留的 Product/Offer 数量，以及绑定
Workflow ID、当前 revision 和完整记录清单指纹的确认 token。只有再次针对同一 Workflow 显式提交完全
匹配的 token 才能执行；执行前在 SQLite 写锁内重新计算清单，任何变化都会拒绝旧计划。删除以 Request
聚合为边界并依赖外键级联清除工作流数据，只回收没有外部 Evidence、评分、报告或额外 Offer 引用的
Product/Offer。一个 Request 若意外关联多个 Workflow 必须拒绝操作，不能扩大删除范围。

该能力只提供本地 CLI，不注册 MCP 删除工具，也不提供清空全部、按日期自动清理、降级或重建数据库。
结构化决策数据默认长期保留，直到用户针对单个 Workflow 明确删除；原始 HTML 和截图缓存仍未启用，
因此计划中的 7/30/14 天缓存保留期尚无需要后台清理的对象。

M6 第五个切片完成本地敏感文件与发布安全审查。文件型 SQLite 数据库必须是非符号链接的普通文件；
POSIX 上还必须没有额外硬链接且不得向 group/other 开放任何权限。`migrate` 在数据库首次打开前以
`0600` 排他创建文件，并可把既有普通数据库修复为 `0600`；新建的直接父目录使用 `0700`。`doctor`
只公开 `private_file_permissions=true|false|null`，不公开路径；POSIX 上权限不安全时即使 Schema 已到 head
也不得报告 ready，默认 MCP 同样拒绝启动。Windows 无统一 POSIX mode 语义时返回 `null`，由用户配置
仅当前账号可访问的系统 ACL。

浏览器专用资料与截图目录必须是非符号链接的私有目录。新目录以 `0700` 创建；已有 POSIX 目录向
group/other 开放时拒绝启动或截图，而不是擅自改变一个可能被复用的目录。截图成功写入后必须验证为
普通单链接文件并收紧到 `0600`，失败时删除可能的局部文件并只抛出脱敏错误。端到端管线仍固定关闭
截图，默认 MCP 仍不启用浏览器。发行安全契约、威胁模型、非目标和事故报告方式记录在 `SECURITY.md`；
CI 的隔离 wheel 验收还必须验证新数据库的私有权限诊断。

### 5.7 本地 AI 宿主与显式京东入口

本地源码检出可以通过 CLI 生成通用 stdio MCP 配置。生成器只接受包含 `pyproject.toml`、`uv.lock` 和
项目包目录的现有项目根目录，输出 `uv --directory ... run --locked` 形式的 JSON；它不读取或输出模型
密钥、Cookie、浏览器会话或数据库内容。默认配置继续启动离线 `personal-shopping-agent-mcp`。

Claude Desktop 安装器 `install-claude-desktop` 只写入宿主官方配置文件（Windows
`%APPDATA%\Claude\claude_desktop_config.json`，macOS `~/Library/Application Support/Claude/`）中名为
`personal-shopping-agent` 的一个 `mcpServers` 条目，其他服务器与设置原样保留。已有文件不是合法 JSON 对象或
`mcpServers` 不是对象时拒绝写入；内容不变时不写；改写前先以独占方式创建带时间戳的完整备份，再原子替换。
输出只包含本项目条目、状态、配置与备份路径，不回显其他服务器配置（可能含密钥）。`--dry-run` 只预览。
`scripts/bootstrap.py --claude-desktop [--live-jd]` 在初始化成功后调用同一命令；仓库根目录的
`install-windows.cmd` 只按顺序调用这些公开命令（uv 缺失时先征得同意再运行 uv 官方安装脚本），不包含
其他逻辑，登录仍由用户在可见浏览器中亲自完成。

MCP 还提供一个静态购物需求准备 Prompt，指导宿主先查询能力，再从自然语言中提取品类、预算、地区和
条件；信息不足时必须追问。Prompt 只是宿主引导，不执行工具、不访问网络，也不能绕过 Pydantic 输入
校验或工作流状态机。

真实京东入口使用独立的 `personal-shopping-agent-mcp-jd` 命令，并要求运行环境显式设置
`PERSONAL_SHOPPING_ENABLE_LIVE_JD=true`。它只组合已经存在的 `BrowserManager`、精确 JD 搜索/商品域名
策略、`ControlledPageCollector`、京东解析器、可恢复管线和“官方来源缺失”提供器。浏览器必须在 MCP
lifespan 开始时启动并在所有退出路径关闭；截图仍由管线固定关闭，访问控制、验证码、403/429 和解析
失败继续停止任务而不是绕过。

该入口不改变默认服务器，也不宣称离线 fixture 能证明真实页面长期可用。CI 只验证配置、生命周期、
工具能力和固定页面回归；首次真实运行必须由用户在电脑上安装 Chromium、确认平台条款并进行人工验收。
真实页面结构变化、需要登录或平台限制时，系统必须报告受限状态，不能伪装为成功比较。

## 6. 确定性工作流

首版使用显式状态机，不使用通用 Agent 图框架：

```text
request_received
  -> request_validated
  -> candidates_discovered
  -> offers_collected
  -> evidence_cross_checked
  -> data_normalized
  -> candidates_scored
  -> report_rendered
  -> completed
```

每个状态定义输入、输出、失败类型、可重试性和审计事件。LLM 调用失败或输出校验失败时，应回退到规则解析或要求用户补充信息，不能让未验证文本进入核心排序。

M2 的状态持久化采用乐观修订号和追加式事件：

- 只有状态机声明的唯一后继状态可以正常推进，禁止跳级和倒退；
- `completed` 与 `failed` 是终态；
- 每次推进必须同时更新当前快照并追加带序号的审计事件；
- 并发写入必须比较修订号，过期写入不得覆盖较新的状态；
- 失败只保存结构化错误码和脱敏说明，不保存原始异常或外部页面内容。

M2 的 MCP 接口只允许创建本地任务和读取状态，不暴露任意状态推进工具。后续平台采集、证据核验、标准化和评分模块只能在相应步骤真实完成后从应用层推进状态。
M5 增加报告生成、读取和显式解释工具；报告生成只能由真实的报告用例从 `candidates_scored` 推进，仍不
提供调用方指定目标状态的通用工具。
M6 增加条件注册的端到端管线工具；它只能调用真实用例推进唯一合法后继状态，并从最后一个持久化
成功状态恢复。默认服务器不注册该工具，终态失败也不会被自动重试或改写。

## 7. 浏览器与平台约束

- 只访问用户请求所需、正常浏览可到达的页面；
- 使用清晰的速率限制、超时、重试上限和缓存；
- 浏览器顶层页面导航采用精确主机白名单，只允许 HTTPS 标准端口；导航前、每次主框架跳转和最终 URL 都必须
  校验目标；
- 页面自身加载的脚本、样式、图片、数据请求和子框架使用平台声明的注册域名列表（如京东的 `jd.com`、
  `360buyimg.com`、`3.cn`）逐个校验，同样只允许 HTTPS 标准端口和公网地址；子资源违规只拦截该资源，不能
  掩盖主页面的真实失败原因；
- DNS 解析结果只要包含私网、本机、链路本地、保留或其他非公网地址，就拒绝该请求；
- 下载以及 checkout/order/pay/payment/trade 等交易路径必须在浏览器层阻止；
- 不绕过验证码、反机器人机制、付费墙、登录限制或访问控制；
- 不逆向私有 API，不执行页面生成的 JavaScript、SQL、Shell 或工具指令；
- 解析器变更必须附带脱敏的固定 HTML/JSON fixture 和回归测试；
- 平台策略变化导致无法可靠采集时，记录受限状态并让用户人工提供信息。

M3 的 Chromium 使用专用本地资料目录，以支持需要用户亲自完成的首次登录或验证码。登录只能由用户通过本地 CLI
`personal-shopping-agent login <平台>` 在有界面的浏览器中亲自完成：代码不读取、不填写、不保存账号密码，
不自动处理验证码或滑块，登录状态只存在于私有专用资料目录。采集过程中主框架被重定向到平台声明的登录主机时，
浏览器立即以 `sign_in_required` 停止并提示用户运行登录命令，而不是继续访问或尝试绕过。京东账号安全验证页 `aq.jd.com` 同样视为需要用户本人处理的步骤：登录窗口允许打开它（登录风控脚本所在的 `jdpay.com` 也只在登录窗口中放行），采集过程中遇到它则以 `sign_in_required` 停止。页面在
`domcontentloaded` 后最多再等待一个有上限的网络空闲窗口，以便读取由页面脚本渲染出的价格，超时不视为失败。该目录、截图、Cookie 和其他会话材料不得进入 Git、数据库、测试 fixture 或日志。基础浏览器只返回有大小上限的 HTML 快照和可选截图路径，不向 MCP 或模型暴露 Page 对象、任意 JavaScript 执行或通用点击能力。平台适配器必须提供自己的最小白名单；未配置平台授权时不启用真实采集。

页面采集前使用平台无关的访问协调器执行按精确主机隔离的低频串行访问。默认同一主机两次尝试至少
间隔 3 秒，单次请求默认最多 2 次且硬上限为 3 次；退避等待有明确上限。只有浏览器的脱敏瞬时导航
故障和预先声明的 0/408/425/5xx 状态可重试。401、403、429、普通 4xx、导航策略拒绝、HTML 大小
拒绝以及解析器发现的验证码、安全验证或访问频率限制都不得自动重试。

带截图的请求只尝试一次，且浏览器仅对 2xx/3xx 响应生成截图，避免为拒绝页和失败重试留下额外
文件。同时发生的相同非截图 URL 可以共享同一个进行中的请求；任务结束后立即释放引用，不保留已
完成页面。该机制只是并发去重，不是 HTML 缓存，不改变原始 HTML/截图缓存继续禁用的隐私边界。
每次实际尝试仍由 BrowserManager 重新执行导航前、子请求和最终 URL 校验，限速或重试层无权放宽
主机、网络、路径或下载策略。

首个平台适配器计划为京东，但所有平台代码必须实现通用端口，不能把平台字段泄漏到领域模型。

### 7.1 首个平台策略

v1.0 先实现京东；闲鱼按 7.2 作为下一个平台，淘宝/天猫或拼多多暂不接入。京东适配按以下顺序完成：

1. 搜索候选：读取 SKU、标题、展示价、店铺、评价摘要和页面徽标；
2. 商品详情：读取规格、变体、卖家、地区价格、库存及明确优惠条件；
3. 证据核验：以品牌官方规格和平台详情交叉核对关键字段；
4. 本地回放：真实访问前后都保存脱敏 fixture，用回归测试检测页面结构变化；
5. 应用接入：只有采集、校验和持久化同时成功，工作流才能推进。

搜索结果属于未验证的 `PlatformCandidate`，不是稳定商品身份，也不是完整报价。只有详情页数据完成 SKU/变体/卖家/地区/采集时间绑定并通过领域校验后，才能形成 `Product` 与 `Offer`。徽标与页面营销词按原始观察保存，不能未经核验直接推断为官方店、绝对正品或最终优惠。

详情页首先形成未验证的 `PlatformProductDetail`，并遵守以下字段规则：

- `list_price`、`displayed_price` 与 `conditional_price` 分别保存，不从其中推测 `unconditional_price` 或 `estimated_total_cost`；
- 条件价必须同时保存页面可见的促销条件，最终结算价仍由用户确认；
- 变体选项必须保留属性名、属性值、目标 SKU 和选中状态，不把不同 SKU 的价格混为同一报价；
- 店铺类型只根据平台控制的明确标签或精确店铺后缀分类，同时保留分类依据；店铺标签不是商品真伪证明；
- 地区、库存原文与采集时间必须保留；只有“有货/现货/无货/下柜”等明确表述才能归一化，其他配送文字保持未知；
- 品牌、型号和其他规格来自详情规格区的原始键值，在官方规格交叉核验前仍是平台观察。

平台适配层预留通用端口，未来可以增加淘宝/天猫或拼多多，但启用前必须分别完成官方 API/授权条件、页面访问边界、字段口径和 fixture 回归测试。不得因为已有京东实现而复制选择器或假设其他平台含义一致。

### 7.1.1 用户辅助采集（M9，默认方式）

2026-09 首次实网验收中，京东风控识别自动化浏览器并以“访问频繁导致无法搜索”替换结果（见
`docs/LIVE_JD_ACCEPTANCE.md`）。按安全规则不做指纹伪装或其他绕过，也不让通用 LLM 代理模拟人工点击；项目
所有者选定“用户浏览、程序分析”：

- 用户在自己的浏览器中正常搜索并打开商品页，以“网页，全部”（`.html`）或“网页，单个文件”（`.mhtml`）
  保存到 `data/inbox/`（Git 忽略）。这些页面是用户本人的浏览结果，程序不发起任何网络请求；
- `SavedPageCollector` 实现与浏览器相同的 `PageCollector` 端口，因此搜索、详情、转换、核验、规范化、
  评分与报告全部复用既有用例。页面原始 URL 取自浏览器写入的 `saved from url` 注释、MHTML 的
  `Snapshot-Content-Location`/`Content-Location` 或 `link rel=canonical`；搜索页按主机与规范化关键词精确
  匹配，商品页按 `item.jd.com/<SKU>.html` 精确匹配；采集时间取 MHTML `Date` 头，否则取文件修改时间；
- 需要的页面尚未保存时以 `page_not_saved` 停止并给出应打开的确切 URL；工作流保持在上次提交的阶段，
  用户保存后重新运行即可继续。只读工具 `list_pages_to_save` 在搜索页已保存时列出管线将读取的商品页
  及其保存状态，方便一次性保存；
- 保存的文件是不可信数据：只接受普通文件（拒绝符号链接），单文件最大 10 MB，只解析不执行，同一 URL
  有多份时取最新一份；
- 该模式不访问网络，因此在默认 MCP 服务器中启用，`run_shopping_pipeline` 在此模式下标注为不访问开放
  网络；浏览器自动采集保留在显式京东入口中，默认安装不再启用。

### 7.2 闲鱼（计划中，M10）

项目所有者已确认以“合规自研”方式接入闲鱼，仅供个人使用、不商业化。参考开源项目 ai-goofish-monitor
的需求转筛选思路，但不采用其代理池/多账号轮换，也不采用 xianyu-hunter 的 mtop 请求拦截与签名重算：

- 用户在专用浏览器资料中亲自登录，建议使用非主账号；不做多账号或代理轮换、指纹伪装、stealth 注入；
- 只解析页面已经渲染的内容，不调用、构造或改写平台内部接口及其签名；
- 同一主机默认间隔不低于京东，并限制单次任务的搜索页和详情页数量；出现滑块、验证、登录或频率提示
  立即停止并交还用户；连续失败后停止任务而不是重试；
- 实现前先扩展领域模型：二手 Offer 需要成色、个人/商家卖家、卖家信用、是否可议价、发货地和发布时间，
  并定义二手风险系数；二手报价不得在没有独立风险口径时与新品报价混排为同一价格结论；
- 与京东相同，必须提供脱敏 fixture 和回归测试，真实页面由用户在自己的电脑上验收。

### 7.3 芯片性能参考（独立评测证据）

芯片性能不是页面上的“数字 + 单位”规格，因此不从平台文字推测，而是用独立评测机构的公开结果换算。
项目所有者选定极客湾 SOCPK“手机芯片综合性能排行”（CPU 权重 70%、GPU 权重 30%，以骁龙 865 为
100；不含功耗、AI、ISP 与基带）。2026-09 起旧地址 `/allperf/?brand=phone` 跳转到首页，该排行改为
`https://www.socpk.com/chart/chip-overall`，列在站点“历史项目”分类下（`/category/mobile-soc` 只有能效
曲线，没有综合分）。新页面不只收录手机：同一榜单还包含 iPad 的 M 系列、Switch 的 Tegra、树莓派等芯片，
这些条目只会在平台规格精确写出同名芯片时匹配：

- 本地 CLI `benchmark refresh` 在用户电脑上用受控浏览器打开一次公开排行页，只读取页面渲染后的条形图行
  （每行的芯片名与分数文字）和页面标题下的方法说明，不解码、不执行、不改写其数据脚本，也不直接请求其数据
  文件；页面标题必须是“综合性能排行”，结构相同的 CPU/GPU 分项排行不会被误存为综合分；渲染出的芯片少于
  20 个时视为未完整加载，不保存。页面主机为 `www.socpk.com`，资源只允许同一注册域名。建议每月最多刷新一次；
- 结果连同来源 URL、采集时间与方法说明保存为本机私有 JSON（`0600` 文件、`0700` 目录，默认
  `data/benchmarks/socpk-allperf.json`，不进入 Git），只用于用户个人决策，不再发布；
- 证据核验阶段读取商品平台规格中声明的芯片键（如“CPU型号”“处理器”），按固定规则折叠厂商前缀、空白与
  “第 N 代骁龙 8”写法后与排行名称**精确**匹配；排行中的“型号(别名)”两种写法都可匹配。匹配成功才追加一条
  `independent_review` 证据 `specifications.chip_performance = "<分数> SOCPK"`，并在备注中保留原始芯片文字
  与排行名称；多个不同芯片分别追加，由规范化阶段显式标记冲突；
- 没有芯片规格、未收录或名称不能精确匹配时不追加任何证据，评分阶段按缺失处理并在报告中显示，
  不做模糊匹配或同代推断；同一折叠名称对应多个不同分数（如 `M4 (4+6)` 与 `M4 (3+6)` 都可写作 M4）时同样
  视为不能唯一匹配；
- 参考文件缺失或校验失败时不启用该证据，其余流程照常进行。

## 8. 隐私与安全

- 日志不得包含密码、令牌、Cookie、完整地址、支付信息或可复用会话数据；
- 敏感值进入系统 Keyring 或进程环境，不写入仓库、数据库快照或测试 fixture；
- 报告和证据快照默认保存在本地，导出前提示可能包含的个人信息；
- 所有外部文本按数据处理，提示注入不得改变工具权限、策略或执行路径；
- 错误信息使用结构化、脱敏的错误码和上下文。

计划中的默认证据保留期为：结构化证据长期保留；搜索页 HTML 7 天；商品详情 HTML 30 天；截图 14 天。
结构化数据可通过本地 CLI 按单个 Workflow 预览、私密导出并立即删除；共享或归属不明确的 Product/Offer
采用保守保留并在预览中计数。包含地址、账号或其他个人信息的页面不得保存完整 HTML，结算与支付页面
不得保存截图。M3 当前只持久化不含页面原文的结构化观察。原始 HTML 与截图缓存仍未启用，必须在实现
对应的定时清理和个人信息阻断规则后才能启用。

## 9. 里程碑

- M0 工程基础：包布局、uv、Ruff、Pyright、pytest、CI、健康检查；
- M1 领域与存储：Product/Offer/Evidence/Request 模型、SQLite、迁移；
- M2 MCP 与编排：只读决策工具、状态机、可恢复错误；
- M3 浏览器与平台：受控 Playwright、京东适配器、脱敏 fixture；
- M4 决策引擎：标准化、证据置信度、价格、风险、Pareto 与排名；
- M5 LLM 与报告：结构化解析、解释、Markdown/HTML 报告；
- M6 发布（已完成）：可恢复端到端装配、端到端测试、安全评审、安装验证、版本化与文档。
- M7 本地可用性（已完成）：MCP 配置生成、自然语言引导 Prompt、显式京东入口与受控浏览器生命周期。
- M8 模块化（已完成）：按需求、查找、呈现、自动化、自动进化五层重组代码，增加确定性需求审查、
  本地脱敏改进案例预览和架构依赖检查。
- M9 京东真实环境就绪（进行中）：分离页面与子资源白名单、登录检测、用户手动登录命令、首次真实测试，
  以及因风控拦截而新增的用户辅助采集（7.1.1）。
- M10 闲鱼（计划中）：按 7.2 扩展二手报价模型并实现合规适配器。

## 10. 变更纪律

- 领域规则变化必须有单元测试和报告解释；
- 排名公式变化必须提供前后样例，防止低能力商品因低价钻空子；
- 数据库和外部契约变化必须可迁移；
- 新依赖要说明用途、维护状态和安全影响；
- 任何超出产品边界的能力必须先修改本设计并由项目所有者确认。
